MUSICBRAINZ_USERAGENT=      #AppName/1.1.1 (github.com/YourUsername)

SLSKD_URL=                  #http://slskd:5030
SLSKD_APIKEY=               #ApiKey1234_here

# where slskd puts completed downloads, as seen from inside THIS container.
# must point at the same files slskd writes to (mount the same volume).
SLSKD_DOWNLOAD_PATH=        #/downloads

# where organized music gets filed to
LIBRARY_PATH=               #/music

# where the jimbrainz database lives, needs to be on a persistent volume
DB_PATH=                    #/config/jimbrainz.db

# off      - never organize, downloads just stay in slskd's folder
# dry_run  - log what would happen, touch nothing  (DEFAULT)
# copy     - copy into LIBRARY_PATH, leave slskd's copy alone
# move     - move into LIBRARY_PATH
#
# starts at dry_run on purpose: organizing is the only thing here that writes to your
# filesystem, so check the event log looks right before switching to copy or move.
ORGANIZE_MODE=              #dry_run
