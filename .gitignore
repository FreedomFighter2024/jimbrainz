.venv/
env/
venv/
.env

.idea
__pycache__/
*.py[cod]
.vscode/

docker-compose.yml

# anchored: this is for a scratch dir at the repo root. Unanchored, `test/` matched ANY
# directory named test at any depth, which silently swallowed ui/test/ - a committed file that
# CLAUDE.md referred to and that simply wasn't there.
/test/
TODO.md

.dockerignore
.devdata/

# dev scaffolding CLAUDE.md already describes as ignored, which it wasn't
run_dev.sh
.claude/

# frontend: node_modules, and the built bundle - generated by ui/, built into the image by
# the dockerfile's ui stage, never committed
node_modules/
interface/dist/

.DS_Store
